% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_building.R
\name{build_tree_from_sequence}
\alias{build_tree_from_sequence}
\title{Build a tree from barcode sequences}
\usage{
build_tree_from_sequence(
  sequence = NULL,
  P_or_list = NULL,
  division = NULL,
  tabs_list = NULL,
  distance_matrix = NULL,
  gRNA.num = 200,
  barcode.label = 1,
  method = c("NJ", "UPGMA"),
  add_C0 = TRUE,
  root_outgroup = "C0",
  drop_outgroup = TRUE,
  resolve.root = TRUE,
  idx0 = 1,
  missing_label = "MISSING",
  show_progress = TRUE
)
}
\arguments{
\item{sequence}{Cell-by-position matrix or data frame of observed barcode
states. Missing values should be \code{NA}.}

\item{P_or_list}{Optional square transition matrix, or list of transition
matrices. Used to build \code{tabs_list} when \code{tabs_list} and \code{distance_matrix}
are not provided.}

\item{division}{Number of divisions used to build the distance dictionary.
Required when \code{P_or_list} is provided.}

\item{tabs_list}{Optional matrix dictionary or list of per-position
dictionaries from \code{build_pair_loglik_tables()}.}

\item{distance_matrix}{Optional precomputed distance matrix. If provided,
dictionary and distance calculation are skipped.}

\item{gRNA.num}{Number of barcode positions to use from \code{sequence}.}

\item{barcode.label}{Root barcode state used when adding \code{"C0"}.}

\item{method}{Tree reconstruction method, either \code{"NJ"} or \code{"UPGMA"}.}

\item{add_C0}{Logical; if \code{TRUE}, add root row \code{"C0"} before computing the
distance matrix.}

\item{root_outgroup}{Outgroup label, usually \code{"C0"}.}

\item{drop_outgroup}{Logical; if \code{TRUE}, remove \code{root_outgroup} from the final
tree/clustering.}

\item{resolve.root}{Logical passed to \code{ape::root()} for NJ rooting.}

\item{idx0}{Initial/root state index in each transition matrix; usually \code{1}.}

\item{missing_label}{Label used for missing observations in \code{tabs_list}.}

\item{show_progress}{Logical; if \code{TRUE}, show progress while computing
distances.}
}
\value{
A list with three components:
\describe{
\item{tree}{The reconstructed tree.}
\item{distance}{The distance matrix used to reconstruct the tree.}
\item{dictionary}{The distance dictionary used to compute the matrix, or
\code{NULL} if \code{distance_matrix} was provided directly.}
}
}
\description{
High-level user-facing wrapper for barcode tree reconstruction. Depending on
the inputs provided, it can start from transition matrices, precomputed
distance dictionaries, or a precomputed distance matrix.
}
\details{
The full workflow is:
transition matrix/list -> distance dictionary -> distance matrix -> tree.
}
