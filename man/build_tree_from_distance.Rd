% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_building.R
\name{build_tree_from_distance}
\alias{build_tree_from_distance}
\title{Build a tree from a distance matrix}
\usage{
build_tree_from_distance(
  distance_matrix,
  method = c("NJ", "UPGMA"),
  root_outgroup = "C0",
  drop_outgroup = TRUE,
  resolve.root = TRUE
)
}
\arguments{
\item{distance_matrix}{Square named distance matrix.}

\item{method}{Tree reconstruction method, either \code{"NJ"} or \code{"UPGMA"}.}

\item{root_outgroup}{Outgroup label, usually \code{"C0"}.}

\item{drop_outgroup}{Logical; if \code{TRUE}, remove \code{root_outgroup} from the final
tree/clustering.}

\item{resolve.root}{Logical passed to \code{ape::root()} for NJ rooting.}
}
\value{
A rooted \code{phylo} object for \code{"NJ"} or an \code{hclust} object for
\code{"UPGMA"}.
}
\description{
Reconstructs a tree from a named distance matrix using either neighbor joining
or UPGMA. For NJ, the tree can be rooted by an outgroup such as \code{"C0"} and the
outgroup can optionally be dropped after rooting. For UPGMA, the outgroup is
removed before clustering by default.
}
