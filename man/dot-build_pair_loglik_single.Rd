% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance_dictionary.R
\name{.build_pair_loglik_single}
\alias{.build_pair_loglik_single}
\title{Build one pairwise log-likelihood distance dictionary}
\usage{
.build_pair_loglik_single(P, division, idx0 = 1, missing_label = "MISSING")
}
\arguments{
\item{P}{Square transition matrix. Row and column names should be state labels.}

\item{division}{Number of divisions used to propagate the initial state.}

\item{idx0}{Initial/root state index in \code{P}; usually \code{1}.}

\item{missing_label}{Label used for missing observations in the dictionary.}
}
\value{
A \verb{(S + 1) x (S + 1)} matrix of negative log-likelihood distances,
where \code{S = nrow(P)}. The final row and column are named \code{missing_label}.
}
\description{
Builds a pairwise distance dictionary for one transition matrix. The output
includes one additional row and column for missing observations, so observed
states and missing states can be scored with the same lookup table.
}
\keyword{internal}
